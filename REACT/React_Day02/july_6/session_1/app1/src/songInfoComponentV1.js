// template to be rendered (displayed) into the div having id: app
const template = 'This is a song info component'

// grab the element having id as app
const element = document.getElementById('app')

// render the template in the element
ReactDOM.render(template, element)
